{
  "name": "rijs.resdir",
  "version": "0.1.0",
  "main": "dist",
  "browser": "./dist/client.js",
  "author": "Pedram Emrouznejad (https://github.com/pemrouz)",
  "scripts": {
    "ignore:1": "find ./dist -type f -exec sed -i \"s/var _interopRequire/\\/* istanbul ignore next *\\/\\nvar _interopRequire/g\" {} ;",
    "ignore:2": "find ./dist -type f -exec sed -i \"s/if (client)/\\/* istanbul ignore next *\\/\\nif (client)/g\" {} ;",
    "reset": "sh -c \"echo '/* istanbul ignore next */' > ./resources/foo.js\" && sh -c \"echo 'module.exports = function foo(){ }' >> ./resources/foo.js\"",
    "babel": "babel src -d dist",
    "bundle": "cp ./node_modules/utilise/identity.js dist/client.js",
    "minify": "uglifyjs dist/client.js -m -c drop_console,dead_code -d debug=false -o dist/client.min.js",
    "build": "npm run babel && npm run ignore:1 && npm run ignore:2 && npm run bundle && npm run minify",
    "test": "npm run reset && istanbul test ./node_modules/mocha/bin/_mocha --report html -- -R spec",
    "coverage": "npm run reset && istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && rm -rf ./coverage",
    "cov": "npm run reset && istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec",
    "version": "npm run build && git add -A",
    "postversion": "git push && git push --tags"
  },
  "license": "pemrouz.mit-license.org",
  "devDependencies": {
    "babel": "^4.6.3",
    "browserify": "^9.0.3",
    "chai": "^2.3.0",
    "rijs.core": "*",
    "rijs.css": "*",
    "rijs.fn": "*",
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.13",
    "mocha": "^2.2.4",
    "mocha-lcov-reporter": "0.0.2",
    "uglify-js": "^2.4.16"
  },
  "dependencies": {
    "chokidar": "^1.0.5",
    "glob": "^5.0.14",
    "utilise": "*"
  }
}
